# Authentication Routes

This folder contains authentication-related pages using Next.js route groups.

## Structure

- `login/` - Login page for user authentication
- `layout.tsx` - Auth layout (no navigation, centered form)

## Route Group

The `(auth)` folder is a route group that doesn't affect the URL structure but allows for a shared layout.
